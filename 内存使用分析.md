# 内存使用分析与优化

## 内存增加的原因分析

### 1. 相机图像加载

**原因**：
- 原始代码中，相机在初始化时默认会将图像加载到GPU内存中
- 当区域较大时，一次性加载所有相机图像会导致GPU内存使用激增
- 即使使用批量加载，也会在初始化时创建所有相机对象，占用CPU内存

**相关代码**：
```python
# 原始Camera类初始化
def __init__(self, colmap_id, R, T, FoVx, FoVy, image, gt_alpha_mask,
             image_name, resolution_scale, uid,
             trans=np.array([0.0, 0.0, 0.0]), scale=1.0, data_device = "cuda",
             load_image=True  # 默认加载图像到GPU
             ):
```
<mcfile name="scene/cameras.py" path="/workspace/scene/cameras.py"></mcfile>

### 2. 模型初始化和训练

**原因**：
- GaussianModel的初始化会创建网络和参数，占用GPU内存
- 训练过程中，梯度计算和优化器状态会占用额外内存
- 点云处理和密度优化会临时占用内存

**相关代码**：
```python
gaussians = GaussianModel(
    dataset.feat_dim, dataset.n_offsets, dataset.fork, dataset.use_feat_bank, dataset.appearance_dim, 
    dataset.add_opacity_dist, dataset.add_cov_dist, dataset.add_color_dist, dataset.add_level, 
    dataset.visible_threshold, dataset.dist2level, dataset.base_layer, dataset.progressive, dataset.extend
)
```
<mcfile name="trian9.py" path="/workspace/trian9.py"></mcfile>

### 3. 区域切换时的内存管理

**原因**：
- 区域切换时，如果不释放上个区域的内存，会导致内存叠加使用
- 加载新区域的相机时，如果GPU内存没有完全释放，会导致内存使用增加

**相关代码**：
```python
# 区域切换时的内存管理
scene.release_images()
torch.cuda.empty_cache()
scene.load_cameras_by_ids(current_camera_pool)
```
<mcfile name="trian9.py" path="/workspace/trian9.py"></mcfile>

### 4. 其他内存使用点

**原因**：
- 数据结构的创建和存储（如相机池、区域配置等）
- 临时变量和中间计算结果
- Python解释器和系统开销

## 内存优化策略

### 1. 相机加载优化

**优化措施**：
- 将Camera类的默认`load_image`参数改为`False`，初始化时不加载图像到GPU
- 修改`cameraList_from_camInfos`函数，确保所有相机都不默认加载图像
- 实现按需加载，只在需要使用相机时才加载图像

**优化效果**：
- 初始化时内存使用显著降低
- 避免了不必要的内存占用
- 内存使用更加可控

**相关代码**：
```python
# 修改后的Camera类初始化
def __init__(self, colmap_id, R, T, FoVx, FoVy, image, gt_alpha_mask,
             image_name, resolution_scale, uid,
             trans=np.array([0.0, 0.0, 0.0]), scale=1.0, data_device = "cuda",
             load_image=False  # 默认不加载图像到GPU
             ):

# 修改后的cameraList_from_camInfos函数
def cameraList_from_camInfos(cam_infos, resolution_scale, args):
    camera_list = []

    for id, c in enumerate(cam_infos):
        # 所有相机都不默认加载图像到GPU，需要时再加载
        load_image = False
        camera_list.append(loadCam(args, id, c, resolution_scale, load_image=load_image))

    return camera_list
```
<mcfile name="scene/cameras.py" path="/workspace/scene/cameras.py"></mcfile>
<mcfile name="utils/camera_utils.py" path="/workspace/utils/camera_utils.py"></mcfile>

### 2. 内存监控与管理

**优化措施**：
- 添加内存监控函数，实时了解内存使用情况
- 在关键位置添加内存监控，如初始化、区域切换等
- 集成内存监控到进度条，避免单独打印

**优化效果**：
- 内存使用透明化，便于识别内存瓶颈
- 及时发现内存异常情况
- 为进一步优化提供数据支持

**相关代码**：
```python
def monitor_memory():
    """监控当前内存使用情况，返回内存使用信息"""
    import psutil
    import torch
    
    # 获取CPU内存使用情况
    cpu_mem = psutil.virtual_memory()
    cpu_used = cpu_mem.used / (1024**3)
    cpu_total = cpu_mem.total / (1024**3)
    cpu_percent = cpu_mem.percent
    
    # 获取GPU内存使用情况
    gpu_used = 0
    gpu_total = 0
    if torch.cuda.is_available():
        gpu_used = torch.cuda.memory_allocated() / (1024**3)
        gpu_total = torch.cuda.get_device_properties(0).total_memory / (1024**3)
    
    # 返回内存使用信息
    return {
        'cpu_used': cpu_used,
        'cpu_total': cpu_total,
        'cpu_percent': cpu_percent,
        'gpu_used': gpu_used,
        'gpu_total': gpu_total
    }
```
<mcfile name="trian9.py" path="/workspace/trian9.py"></mcfile>

### 3. 区域切换优化

**优化措施**：
- 区域切换时，先释放上个区域的内存，再加载新区域的相机
- 使用`torch.cuda.empty_cache()`确保GPU内存完全释放
- 加载新区域前检查内存使用情况，确保有足够的内存

**优化效果**：
- 避免区域切换时的内存叠加使用
- 确保内存使用在可控范围内
- 减少内存碎片

**相关代码**：
```python
# 区域切换时的内存管理
mem_info_before = monitor_memory()
print(f"🔄 [Switch] 释放区域 {prev_region_idx+1} 的照片内存...")
print(f"   切换前内存: CPU={mem_info_before['cpu_used']:.1f}/{mem_info_before['cpu_total']:.1f}GB, GPU={mem_info_before['gpu_used']:.1f}/{mem_info_before['gpu_total']:.1f}GB")
scene.release_images()
torch.cuda.empty_cache()
mem_info_after_release = monitor_memory()
print(f"   释放后内存: CPU={mem_info_after_release['cpu_used']:.1f}/{mem_info_after_release['cpu_total']:.1f}GB, GPU={mem_info_after_release['gpu_used']:.1f}/{mem_info_after_release['gpu_total']:.1f}GB")
print(f"✅ 区域 {prev_region_idx+1} 照片内存已释放")

# 加载新区域的所有照片到GPU
current_camera_pool = REGION_CAMERA_POOLS[current_region_idx]
camera_sequence = generate_random_sequence(current_camera_pool)
print(f"🔄 [Switch] 加载区域 {current_region_idx+1} ({current_region['name']}) 的所有相机到GPU...")
scene.load_cameras_by_ids(current_camera_pool)
mem_info_after_load = monitor_memory()
print(f"   加载后内存: CPU={mem_info_after_load['cpu_used']:.1f}/{mem_info_after_load['cpu_total']:.1f}GB, GPU={mem_info_after_load['gpu_used']:.1f}/{mem_info_after_load['gpu_total']:.1f}GB")
print(f"✅ 区域 {current_region_idx+1} 所有相机已加载到GPU")
```
<mcfile name="trian9.py" path="/workspace/trian9.py"></mcfile>

## 内存使用流程

### 1. 初始化阶段

1. **程序启动**：加载必要的库和模块，占用少量内存
2. **场景初始化**：创建Scene对象，加载相机信息，但不加载图像到GPU
3. **模型初始化**：创建GaussianModel对象，占用GPU内存
4. **区域初始化**：加载第一个区域的相机图像到GPU

### 2. 训练阶段

1. **相机选择**：从当前区域的相机池中随机选择相机
2. **渲染和训练**：使用相机进行渲染和训练，占用GPU内存
3. **内存监控**：每1000次迭代更新进度条，显示内存使用情况
4. **区域切换**：释放当前区域的内存，加载新区域的相机

### 3. 内存使用峰值

- **初始化时**：加载第一个区域的相机图像时
- **区域切换时**：加载新区域的相机图像时
- **训练过程中**：批量处理和梯度计算时

## 进一步优化建议

1. **实现真正的按需加载**：使用完相机后立即释放内存，而不是保留在GPU中
2. **调整区域划分**：将大区域划分为更小的子区域，减少每个区域的相机数量
3. **使用混合精度训练**：使用FP16精度，减少内存使用约一半
4. **内存映射**：对于大型数据集，使用内存映射技术，将图像数据存储在磁盘上
5. **分布式训练**：如果有多个GPU，考虑分布式训练，将不同区域分配到不同的GPU上

## 总结

通过以上优化措施，内存使用情况得到了显著改善：

- **初始化时内存使用降低**：相机在初始化时不再加载图像到GPU
- **内存使用更加可控**：只有在需要使用相机时才会加载图像
- **内存使用透明化**：通过内存监控代码，可以实时了解内存使用情况
- **区域切换时的内存管理**：释放上个区域的内存后再加载新区域的相机

这些优化措施应该能够有效解决内存不足的问题，让训练过程更加稳定。