# Octree-GS 项目内存增长详细分析

## 1. 项目概述

这是一个基于 Octree-GS 的 3D 高斯飞溅项目，用于实时渲染。该项目使用 Python 和 PyTorch 实现，并依赖 CUDA 进行 GPU 加速。

## 2. 主要内存来源分析

### 2.1 模型参数 (GaussianModel)

**核心数据结构** (scene/gaussian_model.py):

| 参数 | 形状 | 内存占用估算 |
|------|------|-------------|
| `_anchor` | (N, 3) | 4 * 3 * N 字节 |
| `_offset` | (N, k, 3) | 4 * k * 3 * N 字节 |
| `_anchor_feat` | (N, c) | 4 * c * N 字节 |
| `_scaling` | (N, 6) | 4 * 6 * N 字节 |
| `_rotation` | (N, 4) | 4 * 4 * N 字节 |
| `_opacity` | (N, 1) | 4 * 1 * N 字节 |
| `_level` | (N, 1) | 4 * 1 * N 字节 |
| `_extra_level` | (N,) | 4 * N 字节 |

*注: k 是每个锚点的偏移数 (默认 5)，c 是特征维度 (默认 32)*

**神经网络参数**:

```python
# scene/gaussian_model.py
self.mlp_opacity: 输出维度 (feat_dim -> feat_dim -> k)
self.mlp_cov: 输出维度 (feat_dim -> feat_dim -> 7*k)
self.mlp_color: 输出维度 (feat_dim -> feat_dim -> 3*k)
self.mlp_feature_bank (可选): (view_dim + level_dim -> feat_dim -> 3)
self.embedding_appearance (可选): (num_cameras, appearance_dim)
```

### 2.2 训练状态变量 (Training State)

**训练统计变量** (只在训练期间存在):

| 变量 | 形状 | 说明 |
|------|------|------|
| `opacity_accum` | (N, 1) | 不透明度累积 |
| `offset_gradient_accum` | (N*k, 1) | 偏移梯度累积 |
| `offset_denom` | (N*k, 1) | 偏移统计分母 |
| `anchor_demon` | (N, 1) | 锚点访问计数 |

**优化器状态** (Optimizer State):

Adam 优化器会为每个参数维护额外的状态:
- `exp_avg`: 一阶矩估计
- `exp_avg_sq`: 二阶矩估计

这意味着每个参数的内存消耗会增加 2 倍。

### 2.3 渲染过程中的内存 (Rendering)

**关键函数** (gaussian_renderer/__init__.py):

```python
# generate_neural_gaussians 函数中的临时变量
anchor [visible_mask]       # (M, 3)
feat [visible_mask]         # (M, c)
grid_offsets [visible_mask] # (M, k, 3)
grid_scaling [visible_mask] # (M, 6)
```

**渲染后返回的变量** (render 函数):

```python
xyz                         # (M*k, 3)
color                       # (M*k, 3)
opacity                     # (M*k,)
scaling                     # (M*k, 3)
rot                         # (M*k, 4)
screenspace_points          # (M*k, 3) - 用于梯度计算
```

### 2.4 Scene 加载相关内存 (重点分析)

**相机数据** (scene/cameras.py):

| 组件 | 内存位置 | 说明 | 内存占用 |
|------|----------|------|----------|
| 相机参数 | CPU | 相机姿态、焦距等 | 每个相机 ~1KB |
| 图像数据 | CPU | 原始图像存储 | 每个图像 ~(W*H*3*4) 字节 |
| 图像数据 | GPU (按需) | 渲染时加载 | 每个图像 ~(W*H*3*4) 字节 |
| 相机变换矩阵 | GPU | 视图变换矩阵 | 每个相机 ~(4*4*4) 字节 |

**点云数据** (scene/__init__.py):

| 阶段 | 内存使用 | 说明 |
|------|----------|------|
| 原始点云加载 | CPU | 从 COLMAP 或 PLY 文件加载 |
| 点云采样 | GPU | 保存为 input.ply 时 |
| 八叉树构建 | GPU | `octree_sample` 函数 |
| 锚点生成 | GPU | `create_from_pcd` 函数 |

## 3. 内存增长的关键阶段

### 3.1 Scene 加载阶段 (重点分析)

**Scene 初始化过程** (scene/__init__.py):

1. **场景信息加载**:
   ```python
   # 第46-51行
   if os.path.exists(os.path.join(args.source_path, "sparse")):
       scene_info = sceneLoadTypeCallbacks["Colmap"](args.source_path, args.images, args.eval, args.ds)
   elif os.path.exists(os.path.join(args.source_path, "transforms_train.json")):
       scene_info = sceneLoadTypeCallbacks["Blender"](args.source_path, args.random_background, args.white_background,  args.eval, ply_path=ply_path)
   else:
       scene_info = sceneLoadTypeCallbacks["City"](args.source_path, args.random_background, args.white_background, args.eval, args.ds, undistorted=args.undistorted)
   ```

2. **相机加载** (内存优化设计):
   ```python
   # camera_utils.py 第64行
   load_image = (id == 0)  # 只加载第一个相机的图像到GPU
   camera_list.append(loadCam(args, id, c, resolution_scale, load_image=load_image))
   ```

3. **相机图像管理** (scene/cameras.py):
   ```python
   # 第42-43行：图像默认存储在CPU
   self._image_data_cpu = image.cpu().clone()
   self._gt_alpha_mask_cpu = gt_alpha_mask.cpu().clone() if gt_alpha_mask is not None else None
   
   # 第50-51行：仅在需要时加载到GPU
   if load_image:
       self._load_image_to_gpu()
   ```

4. **点云处理和八叉树构建**:
   ```python
   # 第98-99行
   self.gaussians.set_level(points, self.train_cameras, self.resolution_scales, args.dist_ratio, args.init_level, args.levels)
   self.gaussians.create_from_pcd(points, self.cameras_extent, logger)
   ```

**Scene 内存优化策略**:
- **延迟加载**：相机图像默认存储在CPU，仅在需要时加载到GPU
- **按需加载**：每次只加载一个相机的图像到GPU
- **内存释放**：提供 `releaseCameraMemory` 和 `releaseAllTrainCamerasMemory` 方法

### 3.2 训练阶段 (Training) - 核心增长来源

**锚点生长 (Anchor Growing)** - 最主要的内存增长源!

```python
# train.py 第 189 行
gaussians.adjust_anchor(...)
```

**adjust_anchor 函数 (scene/gaussian_model.py) 的过程**:

1. **梯度统计收集**:
   ```python
   grads = self.offset_gradient_accum / self.offset_denom
   ```

2. **锚点生长 (anchor_growing)**:
   ```python
   # 对每个 LOD 层级，根据梯度阈值添加新锚点
   for cur_level in range(self.levels):
       # 计算当前层级的网格大小
       cur_size = self.voxel_size / (float(self.fork) ** cur_level)
       
       # 选择候选网格坐标
       candidate_mask = (grads >= cur_threshold) & (grads < ds_threshold)
       
       # 去重并添加新锚点
       selected_grid_coords_unique = torch.unique(...)
       
       # 拼接新参数到现有张量
       self._anchor = torch.cat([self._anchor, new_anchor], dim=0)
       # ... 类似操作对其他参数
   ```

3. **剪枝 (Pruning)**:
   ```python
   # 剪去不透明度低的锚点
   prune_mask = (self.opacity_accum < min_opacity*self.anchor_demon)
   self.prune_anchor(prune_mask)
   ```

**关键点**:
- 锚点数量会随着训练迭代逐渐增加，直到达到饱和
- 新锚点在多个 LOD 层级上同时添加
- 每次添加新锚点时，所有参数张量都要进行 `torch.cat` 操作，这会临时分配双倍内存

### 3.3 反向传播阶段 (Backward Pass)

**计算图和梯度存储**:
- 每次前向传播后，PyTorch 会保存计算图
- 反向传播时计算所有参数的梯度
- 对于 `retain_grad=True` 的张量，会额外保存中间梯度

```python
# train.py 第 148 行
render_pkg = render(..., retain_grad=retain_grad)
# retain_grad 为 True 时会保存更多中间变量
```

### 3.4 保存和检查点阶段 (Saving & Checkpoint)

**模型保存**:
```python
# train.py 第 180 行
scene.save(iteration)
# 保存 ply 文件
gaussians.save_ply(...)

# train.py 第 211 行
torch.save((gaussians.capture(), iteration), ...)
# 保存完整检查点，包括优化器状态
```

**检查点保存导致的内存峰值**:
- `torch.save` 会在保存时序列化数据，临时占用内存
- 如果保存到 CPU 内存，可能会进一步增加主机内存使用

## 4. 内存管理策略 (Memory Management)

### 4.1 项目中已有的优化

1. **训练统计变量清理**:
   ```python
   # train.py 第 200-202 行
   if iteration == opt.update_until:
       del gaussians.opacity_accum
       del gaussians.offset_gradient_accum
       del gaussians.offset_denom
       torch.cuda.empty_cache()  # 显式释放缓存
   ```

2. **验证阶段的缓存清理**:
   ```python
   # train.py 第 248 行
   torch.cuda.empty_cache()
   ```

3. **锚点选择和掩膜 (Masking)**:
   - 使用 `_anchor_mask` 只选择可见的锚点
   - `prefilter_voxel` 函数进一步过滤视锥体内的锚点
   - 减少前向传播中处理的锚点数量

4. **梯度累积变量的重置**:
   ```python
   # scene/gaussian_model.py adjust_anchor 函数
   self.offset_denom[offset_mask] = 0
   self.offset_gradient_accum[offset_mask] = 0
   ```

5. **相机内存管理** (Scene 加载优化):
   ```python
   # scene/cameras.py
   def release_image_from_gpu(self):
       """Release GPU memory for this camera's image"""
       if hasattr(self, 'original_image'):
           del self.original_image
           torch.cuda.empty_cache()
           return True
       return False
   ```

6. **批量相机内存管理**:
   ```python
   # scene/__init__.py
   def releaseAllTrainCamerasMemory(self):
       """Release GPU memory for all training cameras"""
       released_count = 0
       for scale in self.resolution_scales:
           if scale in self.train_cameras:
               for cam in self.train_cameras[scale]:
                   if hasattr(cam, 'release_image_from_gpu'):
                       if cam.release_image_from_gpu():
                           released_count += 1
       # Only clear cache once after all releases
       if released_count > 0:
           torch.cuda.empty_cache()
       return released_count
   ```

### 4.2 PyTorch 的默认内存管理

- **CUDA 缓存分配器**: PyTorch 使用缓存机制管理 GPU 内存
- **内存碎片化**: 频繁的分配和释放可能导致内存碎片
- **torch.cuda.empty_cache()**: 释放缓存但不一定解决碎片化问题

## 5. 关键代码位置索引

### 5.1 训练流程 (train.py)

| 行号 | 代码 | 说明 |
|------|------|------|
| 82-90 | 初始化高斯模型和场景 | 初始内存分配点 |
| 121 | update_learning_rate | 学习率更新 |
| 146-148 | prefilter_voxel + render | 渲染过程，大量临时变量 |
| 153-160 | 损失计算 | 损失函数和正则项 |
| 162 | loss.backward() | 反向传播，梯度计算 |
| 183-204 | adjust_anchor + 清理 | 锚点调整和内存清理 |
| 207-208 | optimizer.step + zero_grad | 优化器更新 |

### 5.2 高斯模型 (scene/gaussian_model.py)

| 行号 | 函数 | 说明 |
|------|------|------|
| 50-164 | `__init__` | 初始化模型和 MLP |
| 298-351 | `create_from_pcd` | 从点云创建初始模型 |
| 634-654 | `training_statis` | 收集训练统计信息 |
| 717-850 | `anchor_growing` | 锚点生长的核心逻辑 |
| 852-904 | `adjust_anchor` | 完整的锚点调整流程 |
| 690-702 | `prune_anchor` | 锚点剪枝 |

### 5.3 渲染 (gaussian_renderer/__init__.py)

| 行号 | 函数 | 说明 |
|------|------|------|
| 44-153 | `generate_neural_gaussians` | 生成神经网络高斯参数 |
| 155-224 | `render` | 主渲染函数 |
| 227-274 | `prefilter_voxel` | 视锥体剔除和预过滤 |

### 5.4 Scene 加载 (scene/__init__.py)

| 行号 | 函数 | 说明 |
|------|------|------|
| 26-100 | `__init__` | Scene 初始化，相机和点云加载 |
| 101-106 | `save_ply` | 保存点云为 PLY 文件 |
| 107-111 | `save` | 保存模型检查点 |
| 124-143 | `releaseCameraMemory` | 释放特定相机的 GPU 内存 |
| 144-156 | `releaseAllTrainCamerasMemory` | 释放所有训练相机的 GPU 内存 |
| 158-198 | `reloadCameraImage` 和 `ensureCameraLoaded` | 相机图像管理 |

### 5.5 相机管理 (scene/cameras.py)

| 行号 | 函数 | 说明 |
|------|------|------|
| 17-63 | `__init__` | 相机初始化，图像存储在 CPU |
| 64-72 | `_load_image_to_gpu` | 将图像加载到 GPU |
| 73-79 | `release_image_from_gpu` | 释放 GPU 中的图像内存 |
| 81-84 | `reload_image` | 重新加载图像到 GPU |
| 86-88 | `is_image_loaded` | 检查图像是否在 GPU 上 |

## 6. 内存使用估算示例

假设配置参数:
- 锚点数量: N = 100,000
- 偏移数: k = 5
- 特征维度: c = 32
- 相机数量: 100
- 图像分辨率: 1024x768

**基础参数内存** (不包括优化器状态):
```
_anchor:         100,000 * 3 * 4 = 1.2 MB
_offset:         100,000 * 5 * 3 * 4 = 6 MB
_anchor_feat:    100,000 * 32 * 4 = 12.8 MB
_scaling:        100,000 * 6 * 4 = 2.4 MB
_rotation:       100,000 * 4 * 4 = 1.6 MB
_opacity:        100,000 * 1 * 4 = 0.4 MB
_level/_extra_level: 0.8 MB
MLP 参数: 约 5-10 MB
------------------------
总计: 约 40-50 MB
```

**包括优化器状态 (Adam)**:
- 基础参数: ~50 MB
- 优化器 exp_avg: ~50 MB
- 优化器 exp_avg_sq: ~50 MB
- 训练统计变量: ~几 MB
- **总计: ~150-200 MB**

**相机相关内存**:
- 相机参数 (CPU): 100 * 1KB = 0.1 MB
- 图像数据 (CPU): 100 * 1024*768*3*4 = ~900 MB
- 图像数据 (GPU): 默认只加载 1 个相机 = ~9 MB
- 相机变换矩阵 (GPU): 100 * 4*4*4 = ~6.4 KB

**渲染时的临时内存峰值**:
- 假设可见锚点比例 20%, M = 20,000
- 生成的高斯数量: M * k = 100,000
- 各种渲染中间变量: 约 50-100 MB

## 7. 潜在的内存优化方向

1. **原地操作 (In-place Operations)**:
   - 使用 `in-place` 的激活函数和操作
   - 避免不必要的张量复制

2. **梯度检查点 (Gradient Checkpointing)**:
   - 用计算时间换内存空间
   - 在前向传播时不保存所有中间变量

3. **混合精度训练 (Mixed Precision)**:
   - 使用 FP16 进行训练，减少一半内存
   - PyTorch 的 `torch.cuda.amp` 模块

4. **锚点数量限制**:
   - 设置最大锚点数量限制
   - 更 aggressive 的剪枝策略

5. **内存高效的张量拼接**:
   - 预分配更大的张量缓冲区
   - 避免频繁的小 `torch.cat` 操作

6. **释放不必要的中间变量**:
   - 在训练循环中显式删除不需要的变量
   - 使用 `del` 和 `torch.cuda.empty_cache()` 配合

7. **Scene 加载优化** (重点):
   - **批量相机处理**: 一次只处理少量相机，处理完后释放内存
   - **图像压缩**: 考虑使用压缩格式存储图像
   - **点云简化**: 在保证质量的前提下减少初始点云密度
   - **分阶段加载**: 先加载必要数据，其他数据延迟加载

## 8. 监控和调试建议

### 8.1 内存监控代码片段

```python
import torch

def print_memory_usage():
    allocated = torch.cuda.memory_allocated() / 1024**3
    cached = torch.cuda.memory_reserved() / 1024**3
    print(f"GPU Memory - Allocated: {allocated:.2f} GB, Cached: {cached:.2f} GB")

# 在训练循环中添加
for iteration in range(...):
    # ... 训练代码 ...
    if iteration % 100 == 0:
        print_memory_usage()
```

### 8.2 PyTorch 内存分析工具

- `torch.cuda.memory_summary()`: 详细的内存统计
- `torch.cuda.memory_snapshot()`: 内存快照分析
- PyTorch Profiler: 内存 timeline 可视化

---

**总结**: 这个项目的内存增长主要来自于训练过程中的锚点数量增加和 Scene 加载阶段的数据处理。Scene 加载的内存优化设计（如相机图像延迟加载）已经很合理，但仍有进一步优化空间。优化重点应该放在锚点生长策略、内存管理和 Scene 加载的分阶段处理上。